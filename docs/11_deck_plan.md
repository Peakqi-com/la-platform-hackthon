# 11 簡報內容規劃（文字版，待確認後製作）

用途：決賽簡報，依使用流程順序介紹所有功能。每頁列「標題／講什麼／配圖」。配圖檔名對應 `docs/demo/screenshots/`；標「補拍」的是確認後再截的畫面。
預計 30 頁，講 12 分鐘（含 demo 導覽 3 分鐘）。

## 第一段 為什麼（3 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 1 | 封面 | 主標「AI 輔助不動產估價案件審查」、副標「土地徵收補償市價查估・估價案件審查輔助系統」；隊名「AI城市起風」；成員 吳昭奇、洪湛閎、丁家麒；地政局命題 | 主辦 KV `webtitle.jpg`（深藍底、粉紫／青色字）當底，右下放隊名成員 |
| 2 | 現況與痛點（引命題文件原句） | 背景：「審查人員須人工比對多張表單（如地價區段勘查表、影響地價區域因素分析明細表、比較法調查估價表），逐項核算距離、級距、修正率與加總結果，不僅作業時間長，且容易因人工判讀或抄填錯誤影響審查品質」；三個痛點原句：1 表單眾多、人工逐項比對耗時易疏漏；2 區域／個別因素需依不同用地類別基準明細表判定，一致性不易維持；3 修正率加總及跨表抄填容易出現計算或填載錯誤 | 命題文件截圖（補拍）＋範本 PDF 第 1、3 頁縮圖 |
| 3 | 我們做什麼（一句話） | 把「勘查表事實 → 優劣等級 → 修正率 → 加總 → 跨表抄填」這條鏈自動化，並反向審查：比對估價師填的表與規則算出的表，逐條指出不一致並引用依據；沒有書表時只給地號也能先產出一版 | 四步驟圖（輸入→產出書表→審查→輸出），用首頁四張卡片 `01_home.png` 局部 |

## 第二段 設計原則與資料基礎（3 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 4 | 四個不可違反的原則 | ① 數字全由確定性規則引擎算，語言模型不碰數字；② 每條算式對回查估辦法條號／作業手冊頁碼（docs/07）；③ 推定值只標「需確認」不判錯，估價師填值優先；④ 資料在本機，不依賴外部 API 做 demo | 文字頁＋一張 docs/07 對照表局部（補拍） |
| 5 | 法規與規則來源 | 土地徵收補償市價查估辦法、作業手冊（頁碼校正到印刷頁）、不動產估價技術規則（技術規則為一般法，衝突時依查估辦法）、估價師公會第四號公報（建物成本）、內政部評價基準表上限、新北市各計畫區土管要點與施行細則附表一；基準明細表是資料不是程式，換區換用地別只換 JSON | 法規封面拼貼（補拍或用文字） |
| 6 | 圖資與開放資料 | 國土測繪中心底圖／段籍圖（瓦片快取離線可用）、OSM 路網／步行圖／設施／門牌（全新北市）、城鄉局使用分區、地籍圖（匯入圖檔或預載）、實價登錄、都市地價指數、營造工程物價指數、水利署淹水潛勢、環境部列管污染源、經濟部商圈、交通局路邊停車格、高公局交流道、中油加油站；每筆距離帶來源與量測方式 | 首頁頁腳授權標示（補拍）＋設施庫 95,696 筆數字 |

## 第三段 案件總覽與三種入口（5 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 7 | 案件總覽 | 承辦儀表：全部／審查中／待處理不符項／產出已過期／已完成；兩個入口卡（審查送審書表、一鍵建案）；案件清單（審查結果、比較價格、產出狀態、最後操作、下一步）；封存取代刪除；左下操作身分寫進紀錄與落款 | `01_home.png`、案件清單（補拍：三案並列） |
| 8 | 入口 A：上傳送審書表 PDF | 拖進六頁 PDF → 文字層讀取（掃描件走影像辨識）→ 預覽：每頁辨識到哪一表、案號、比準地、比較標的件數、缺漏欄位、低信心欄位、提醒 → 「建立案件並開始審查」 | `02_import_preview.jpg` |
| 9 | 入口 B：一鍵建案（依地號） | 只填案號、基準日、鄉鎮市區、比準地地號、用地別 → 地籍界線 → 宗地屬性推定 → 區段範圍（路網推估街廓）→ 勘查表 → 設施距離 → 實價登錄比較標的，約 30 秒 | `13_onekey_form.jpg` |
| 10 | 入口 C：範例 | 範例一相符、範例二含填載錯誤、範例三僅有勘查表基本欄位；給評審一鍵重現 | 首頁「其他範例」展開（補拍） |
| 11 | 還能上傳什麼 | 宗地個別因素清冊 xlsx、買賣實例 xlsx、評價基準明細表 PDF／CSV／JSON、地籍圖（GeoJSON／KML／GML／SHP zip）、地價區段圖；匯入依地號併入、只覆蓋有值欄位、對不到的回報 | 「還能上傳什麼」展開（補拍） |

## 第四段 ① 輸入資料（5 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 12 | 案件與地價區段 | 案號、基準日、估價師簽章欄、比準地地號、用地別、適用基準表；區段編號、範圍文字、勘查日期；「依地號產生」可覆寫已填值；位置與設施距離區塊（匯入區段圖／地籍圖／補上書表 PDF、重新量測、替代方式：點圖推估街廓、人工標定） | 輸入頁案件分頁（補拍，1140901-99-003） |
| 13 | 填寫結果清單 | 每個欄位標 資料／量測／推定／空白 四種狀態＋來源＋說明；一眼看出哪些要人工補（例：建蔽率沒有細分區時列空白） | `14_fill_report.png` |
| 14 | 宗地條件與買賣實例 | 比準地 19 欄個別因素；比較標的固定三欄並排；實價登錄自動選取（§17 蒐集期間、§19 同區段→同鄉鎮→鄰近鄉鎮、期間外參考案例需理由）；含建物實例：第四號公報成本法推定建物成本（每坪營造施工費 × 坪數 × 定額折舊 × 營造工程物價指數）標需確認；期日調整依都市地價指數內插；無地籍界線時依門牌定位、可點圖設定 | `15_comparables.png` 局部＋宗地分頁（補拍） |
| 15 | 評價基準明細表 | 系統內建：金山商業用地區域／個別、示範住宅用地；匯入地政局基準表 PDF → 自動解析為規則 JSON（細項對回內政部項目目錄，超過內政部上限只提醒）；點名稱看細項與矩陣；「套用至本案」 | 輸入頁基準表分頁（補拍）＋基準表明細（補拍） |
| 16 | 勘查表推定規則 | 28 欄哪些能推：都市計畫內外、使用分區、建蔽容積、道路寬度、車站站牌交流道、道路闢建程度、排水（淹水潛勢）、地勢（高程）、店舖毗連、顧客通行量、污染源、商圈、停車方便性；門檻是系統自訂，全部寫在 docs/07 | docs/07 推定規則表局部（補拍）＋勘查表頁 `16_generated_sheet.png` |

## 第五段 ② 產出書表（4 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 17 | 規則引擎：等級 → 修正率 → 價格鏈 | 勘查表事實依基準表判定條件判等級 → 矩陣查修正率 → 小計 → 總修正數 → 區域因素調整 → 個別因素差異率 → 試算價格 → 權重（絕對值加總排名 50/30/20）→ 比較價格 → 依 §21 尾數進位；驗收：範本每格等級、每個差異率、13.00%、212,958；手冊 p.100–102 算例 | 流程圖（製作）＋ `05_rule_cell.png`（矩陣格位） |
| 18 | 六頁書表預覽 | 照查估書表範本頁序：地價區段勘查表、影響地價區域因素分析明細表、比較法調查估價表、地價區段略圖、地價使用分區圖、地價區段圖；Excel、預覽、PDF 同一份版面；列印六頁 PDF；可疊「審查標記」 | `06_sheets.png` |
| 19 | 三張圖說 | 略圖（區段、宗地、道路、段籍圖底圖、比例尺、簽章欄）、使用分區圖、區段圖（設施位置與量測路線）；A3 橫式 PNG，進 Excel 工作表與意見書附圖 | 圖說 PNG 一張（補拍：map.png?page=1） |
| 20 | 地圖互動檢視 | 三張圖切換、點設施高亮距離線、右側「比準地接近條件與嫌惡設施」每筆標來源與量測方式；比較標的位置可點圖設定 | `07_map.png` |

## 第六段 ③ 審查（4 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 21 | 逐項比對 | 七個審查重點（i～vii）；每列：結果（不符／需確認／備註）、書表、位置、估價單位填載、系統核算、說明、依據（辦法條號／手冊頁碼）、追溯（看基準表格位）；依全案／比較標的分組、依結果篩選；顯示圖例 | `08_review_mismatch.png`、`09_mismatch_rows.png` |
| 22 | 需確認 vs 不符 | 範本案：全部相符＋1 需確認（道路種類最大修正率 8 超過內政部上限 5，範本本身疑點，不判錯）；推定值一律只列需確認 | `03_review_pass.png`、`04_review_confirm_row.png` |
| 23 | 承辦裁決與操作紀錄 | 每項不符：接受填載（填理由）或維持不符；儲存後寫進操作紀錄（時間、操作者、動作、內容）；輸入改動後產出過期、裁決對不到的標過期 | 裁決欄與操作紀錄（補拍） |
| 24 | 審查意見書 | 結論（相符／需確認／不符請補正）、核算摘要、逐條意見 [F-xxx]＋法源＋承辦裁決＋裁決人、附三張圖說、落款；Word／PDF；語言模型潤飾預設關閉，開啟時有守門（標籤齊、沒有新數字，退回原句） | `11_opinion.png` |

## 第七段 ④ 輸出與案件管理（2 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 25 | 輸出 | 下載全部 zip；個別：查估書表 Excel（六張工作表）／PDF、審查意見書 Word／PDF、三張圖 PNG、宗地清冊 xlsx、買賣實例 xlsx（填好可再匯入） | `12_export.png`＋Excel 開啟畫面（補拍） |
| 26 | 案件生命週期 | 草稿→審查中→已完成；輸入最後修改／產出最後產生／產出已過期→重新產生書表；重置（清空重填／回到原始輸入，保留匯入圖檔）；另存新案；封存與復原；操作身分 | 案件列狀態列（補拍） |

## 第八段 技術與部署（3 頁；依「黑客松競賽環境規範與限制 20260722」與 Supported AWS Services List）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 27 | 系統架構 | 前端 Next.js（書表預覽、地圖、審查）→ 後端 FastAPI（規則引擎 engine、空間 spatial、市場 market、報表 report、圖 maps、input adapter）→ 本機資料層（SQLite R-tree 全區圖資依案件範圍局部載入、規則 JSON、案件 JSON、操作紀錄）；語言模型層 Amazon Bedrock（僅意見書潤飾與掃描件辨識，介面可切換 mock）；157 個自動測試（範本驗收、手冊算例、adapter、輸出、API） | 架構圖（製作，套主辦 KV 深藍配色） |
| 28 | AWS 部署（依競賽規範） | 區域 us-east-1／us-west-2；單一 EC2 標準型（t3／m 系列，規範允許 Standard 256 vCPU；不用 GPU、不做模型訓練）跑後端 8000 與前端 3000，Caddy 反向代理 HTTPS；Security Group 只開 443／80（不對外全開）；Bedrock 請求 < 1 RPS（意見書潤飾一次一請求，符合）、只開通用到的模型；不放個人資料（實價登錄為去識別化公開資料；操作身分用職稱）；S3 若用只做私有備份、不開公開存取；機密走環境變數與 .gitignore，repo 私有；30 分鐘 runbook（deploy/runbook_ec2.md） | 部署圖（製作）＋規範重點列表 |
| 29 | 限制與後續 | 收益法（查估辦法 §14）未實作；樓層別效用比率公報無表由估價師填；地籍圖現用匯入圖檔或預載，正式版需國土測繪中心地籍 API（地政局名義申請）；土管要點 8 個計畫區抓不到表退回附表一；勘查表推定門檻為系統自訂待地政局確認；底圖瓦片來自國土測繪中心，境外主機需確認可否連線（已快取金山一帶可離線） | 文字 |

## 第九段 收尾（1 頁）

| # | 標題 | 講什麼 | 配圖 |
|---|---|---|---|
| 30 | Demo 與結語 | 現場走三個場景（送審書表→審查；填載錯誤→裁決→意見書；地號→一鍵產出）；一句話收尾：估價單位送來什麼就逐格核算，沒送也能先產一版，每個數字都能點回法源 | `03_review_pass.png`＋`11_opinion.png`＋`14_fill_report.png` 三小圖 |

## 補拍清單（確認後一次截）

範本 PDF 第 1、3 頁縮圖；首頁頁腳授權；案件清單三案；「其他範例」展開；「還能上傳什麼」展開；輸入頁三個分頁（案件與地價區段、宗地條件與買賣實例、評價基準明細表）；基準表明細（點名稱後）；docs/07 對照表與推定規則表；圖說 PNG；裁決欄＋操作紀錄；Excel 六張工作表開啟畫面；案件列狀態列；架構圖與流程圖（自製）。

## 已確認（2026-09-11）

- 隊名 AI城市起風；成員 吳昭奇、洪湛閎、丁家麒；封面用主辦 KV `webtitle.jpg`。
- 痛點頁引命題文件原句。
- 技術與部署依「黑客松競賽環境規範與限制 20260722」改寫（第 27～29 頁）；金鑰改為 Bedrock。
- 格式 PowerPoint 16:9，配色比照網站（暖橘主色、米白底、深藍輔色），封面與段落頁用 KV 深藍。
