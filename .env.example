# 後端不讀 .env，這些是要 export 到環境的變數（或寫進 systemd 的 Environment=）。資料檔路徑用預設即可，不必設定。
# --- 語言模型（只用於掃描件辨識與意見書潤飾；沒設也能跑，首頁狀態列會顯示未設定）
LLM_PROVIDER=anthropic            # anthropic | bedrock
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL_ID=claude-opus-5
AWS_REGION=ap-northeast-1
BEDROCK_MODEL_ID=
# --- 路徑規劃（選用；沒設就用內建步行路網）
OSRM_URL=
# --- 前端 build 時寫進 rewrites（先起後端再 build）
BACKEND_URL=http://127.0.0.1:8000
# --- 以下只有換資料檔位置時才需要（預設都在 data/ 下）
# ZONING_DB=data/zoning/ntpc_zoning.sqlite      全區使用分區（設 ZONING_GEOJSON 會改用單一 GeoJSON，等於關掉全區庫，不要設）
# ROADS_DB=data/osm/roads.sqlite                全區路網
# WALK_DB=data/osm/walk.sqlite                  全區步行圖
# POI_DB=data/poi_ntpc.sqlite                   設施庫
# CADASTRE_GEOJSON=                             地籍圖（預設 data/cadastre/default.geojson）
# CASES_DIR=data/cases  TILE_CACHE=data/tiles  TERRAIN_OFFLINE=1（會場無外網時）
